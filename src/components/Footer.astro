---
const currentYear = new Date().getFullYear();
const startYear = 2014; // Based on the earliest blog posts mentioned in requirements
const copyrightYears = startYear === currentYear ? currentYear : `${startYear}–${currentYear}`;

const footerLinks = [
  { text: 'RSS Feed', href: '/rss.xml' },
  { text: 'Sitemap', href: '/sitemap-index.xml' }
];
---

<footer class="mt-32 border-t border-gray-200 dark:border-gray-800">
  <div class="mx-auto max-w-screen-xl px-4 py-12">
    <div class="flex flex-col items-center justify-between gap-6 sm:flex-row">
      <p class="text-sm text-gray-600 dark:text-gray-400">
        © {copyrightYears} Cleaver Barnes. All rights reserved.
      </p>
      <nav class="flex gap-6" aria-label="Footer navigation">
        {
          footerLinks.map(({ text, href }) => (
            <a
              href={href}
              class="text-sm text-gray-600 transition hover:text-gray-900 dark:text-gray-400 dark:hover:text-gray-100"
            >
              {text}
            </a>
          ))
        }
      </nav>
    </div>
  </div>
</footer>

